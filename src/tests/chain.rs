#[test]
fn test_create_blockchain() {
    // This test is intentionally left empty.
    use super::*;

    let blockchain = new_blockchain();
    assert_eq!(blockchain.len(), 1);

}

#[test]
fn test_create_proof_of_work() {

    use super::*;

}